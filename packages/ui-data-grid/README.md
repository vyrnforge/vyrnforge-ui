# @your-org/ui-data-grid

Lightweight native-first React + TypeScript Universal Data Grid package foundation.

This Phase 0 package exposes generic contracts, pure core helpers, controlled/uncontrolled state hooks, native table rendering, and base CSS variables. Redux, MUI, TanStack, exporter engines, and domain-specific UI are intentionally not part of the package.
