# @dravyn/ui-core

Shared Dravyn UI foundation package for tokens, CSS variables, density values, and small framework-neutral utilities.

This package is intentionally minimal while `@dravyn/ui-data-grid` remains the first production component.
