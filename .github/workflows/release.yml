name: Controlled npm Release

on:
  workflow_dispatch:
    inputs:
      release-group:
        description: Canonical BT-8002 release group
        required: true
        default: non-grid-beta
        type: choice
        options:
          - non-grid-beta
          - data-grid-alpha
      version:
        description: Exact prerelease version declared for the selected group
        required: true
        type: string
      mode:
        description: Verify the candidate or publish it after approval
        required: true
        default: verify
        type: choice
        options:
          - verify
          - publish
      dist-tag:
        description: npm dist-tag declared for the selected release group
        required: true
        default: beta
        type: choice
        options:
          - beta
          - alpha
          - next

permissions:
  contents: read

concurrency:
  group: vyrnforge-npm-release
  cancel-in-progress: false

jobs:
  compatibility-checks:
    name: compatibility-checks
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/_compatibility.yml
    permissions:
      contents: read

  security-checks:
    name: security-checks
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/_security.yml
    permissions:
      contents: read
      pull-requests: read
      security-events: write

  verify-release:
    name: verify-release
    needs:
      - compatibility-checks
      - security-checks
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    env:
      RELEASE_GROUP: ${{ inputs.release-group }}
      RELEASE_VERSION: ${{ inputs.version }}
      RELEASE_TAG: ${{ inputs.dist-tag }}
      RELEASE_MODE: ${{ inputs.mode }}
      VYRNFORGE_RELEASE_VERSION: ${{ inputs.version }}

    steps:
      - name: Checkout selected commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Confirm current main release candidate
        shell: bash
        run: |
          git fetch origin main --depth=1
          test "$GITHUB_SHA" = "$(git rev-parse origin/main)"

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Use supported npm CLI
        shell: bash
        run: |
          npm install --global "npm@$(node -p 'require("./package.json").packageManager.split("@").pop()')"
          node -e 'const [major, minor] = process.versions.node.split(".").map(Number); if (major !== 24 || minor < 18) process.exit(1)'
          node -e 'const actual = require("child_process").execFileSync("npm", ["--version"], { encoding: "utf8" }).trim(); const expected = require("./package.json").packageManager.split("@").pop(); if (actual !== expected) process.exit(1)'

      - name: Install dependencies
        run: npm ci

      - name: Verify release candidate inputs and registry availability
        run: node scripts/verify-release-candidate.mjs --release-group "$RELEASE_GROUP" --version "$RELEASE_VERSION" --dist-tag "$RELEASE_TAG"

      - name: Install Chromium for browser validation
        run: npx playwright install --with-deps chromium

      - name: Run authoritative release validation
        run: npm run quality

      - name: Verify beta artifacts and approved size budgets
        if: inputs.release-group == 'non-grid-beta'
        run: |
          npm run verify:beta-package-artifacts
          npm run verify:beta-package-size-budgets

      - name: Require complete assistive-technology evidence for beta
        if: inputs.release-group == 'non-grid-beta'
        run: npm run verify:assistive-technology:release

      - name: Verify clean generated-artifact state
        shell: bash
        run: |
          git diff --check
          test -z "$(git status --porcelain --untracked-files=all)"

      - name: Write verification summary
        shell: bash
        run: |
          if test "$RELEASE_GROUP" = "non-grid-beta"; then
            packages="@vyrnforge/ui-core -> @vyrnforge/ui-behaviors -> @vyrnforge/ui-components -> @vyrnforge/ui-elements"
            package_dirs=(packages/ui-core/dist packages/ui-behaviors/dist packages/ui-components/dist packages/ui-elements/dist)
          else
            packages="@vyrnforge/ui-data-grid"
            package_dirs=(packages/ui-data-grid/dist)
          fi
          {
            echo "## VyrnForge npm release verification"
            echo
            echo "- Release group: \`$RELEASE_GROUP\`"
            echo "- Candidate version: \`$RELEASE_VERSION\`"
            echo "- Dist-tag: \`$RELEASE_TAG\`"
            echo "- Requested mode: \`$RELEASE_MODE\`"
            echo "- Commit: \`$GITHUB_SHA\`"
            echo "- Packages: \`$packages\`"
            echo "- Internal dependencies: exact versions from \`docs/metadata/release-groups.json\`"
            echo "- Validation: workflow contracts, metadata, formatting, typecheck, coverage, accessibility, regression fixtures, package payloads, consumer, docs, and playground passed"
            echo "- Registry: candidate versions are not yet published"
            echo "- Credentials: no token secret used; verification did not request OIDC or publish"
            echo
            echo "### Package dist sizes"
            du -sh "${package_dirs[@]}"
          } >> "$GITHUB_STEP_SUMMARY"

  publish-packages:
    name: publish-packages
    if: inputs.mode == 'publish'
    needs: verify-release
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment:
      name: npm-release
    permissions:
      contents: read
      id-token: write
    env:
      RELEASE_GROUP: ${{ inputs.release-group }}
      RELEASE_VERSION: ${{ inputs.version }}
      RELEASE_TAG: ${{ inputs.dist-tag }}
      VYRNFORGE_RELEASE_VERSION: ${{ inputs.version }}

    steps:
      - name: Checkout selected commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Confirm current main release candidate
        shell: bash
        run: |
          git fetch origin main --depth=1
          test "$GITHUB_SHA" = "$(git rev-parse origin/main)"

      - name: Setup Node for trusted publishing
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Use supported npm CLI
        shell: bash
        run: |
          npm install --global "npm@$(node -p 'require("./package.json").packageManager.split("@").pop()')"
          node -e 'const [major, minor] = process.versions.node.split(".").map(Number); if (major !== 24 || minor < 18) process.exit(1)'
          node -e 'const actual = require("child_process").execFileSync("npm", ["--version"], { encoding: "utf8" }).trim(); const expected = require("./package.json").packageManager.split("@").pop(); if (actual !== expected) process.exit(1)'

      - name: Install dependencies
        run: npm ci

      - name: Verify release-critical package state
        run: |
          node scripts/verify-release-candidate.mjs --release-group "$RELEASE_GROUP" --version "$RELEASE_VERSION" --dist-tag "$RELEASE_TAG"
          npm run verify:packages

      - name: Publish ui-core through npm OIDC
        if: inputs.release-group == 'non-grid-beta'
        run: npm publish ./packages/ui-core --access public --tag "$RELEASE_TAG"

      - name: Verify ui-core registry propagation
        if: inputs.release-group == 'non-grid-beta'
        shell: bash
        run: |
          for _ in {1..12}; do
            if test "$(npm view "@vyrnforge/ui-core@$RELEASE_VERSION" version --registry=https://registry.npmjs.org 2>/dev/null || true)" = "$RELEASE_VERSION"; then
              exit 0
            fi
            sleep 5
          done
          echo "ui-core publication did not propagate"
          exit 1

      - name: Publish ui-behaviors through npm OIDC
        if: inputs.release-group == 'non-grid-beta'
        run: npm publish ./packages/ui-behaviors --access public --tag "$RELEASE_TAG"

      - name: Verify ui-behaviors registry propagation
        if: inputs.release-group == 'non-grid-beta'
        shell: bash
        run: |
          for _ in {1..12}; do
            if test "$(npm view "@vyrnforge/ui-behaviors@$RELEASE_VERSION" version --registry=https://registry.npmjs.org 2>/dev/null || true)" = "$RELEASE_VERSION"; then
              npm view "@vyrnforge/ui-behaviors@$RELEASE_VERSION" dependencies --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const dependencies = JSON.parse(input); process.exit(dependencies["@vyrnforge/ui-core"] === process.env.RELEASE_VERSION ? 0 : 1); })'
              exit 0
            fi
            sleep 5
          done
          echo "ui-behaviors publication did not propagate with the expected ui-core dependency"
          exit 1

      - name: Publish ui-components through npm OIDC
        if: inputs.release-group == 'non-grid-beta'
        run: npm publish ./packages/ui-components --access public --tag "$RELEASE_TAG"

      - name: Verify ui-components registry propagation
        if: inputs.release-group == 'non-grid-beta'
        shell: bash
        run: |
          for _ in {1..12}; do
            if test "$(npm view "@vyrnforge/ui-components@$RELEASE_VERSION" version --registry=https://registry.npmjs.org 2>/dev/null || true)" = "$RELEASE_VERSION"; then
              npm view "@vyrnforge/ui-components@$RELEASE_VERSION" dependencies --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const dependencies = JSON.parse(input); process.exit(dependencies["@vyrnforge/ui-core"] === process.env.RELEASE_VERSION && dependencies["@vyrnforge/ui-behaviors"] === process.env.RELEASE_VERSION ? 0 : 1); })'
              exit 0
            fi
            sleep 5
          done
          echo "ui-components publication did not propagate with the expected beta dependencies"
          exit 1

      - name: Publish ui-elements through npm OIDC
        if: inputs.release-group == 'non-grid-beta'
        run: npm publish ./packages/ui-elements --access public --tag "$RELEASE_TAG"

      - name: Verify ui-elements registry propagation and beta dist-tag
        if: inputs.release-group == 'non-grid-beta'
        shell: bash
        run: |
          for _ in {1..12}; do
            if test "$(npm view "@vyrnforge/ui-elements@$RELEASE_VERSION" version --registry=https://registry.npmjs.org 2>/dev/null || true)" = "$RELEASE_VERSION"; then
              npm view "@vyrnforge/ui-elements@$RELEASE_VERSION" dependencies --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const dependencies = JSON.parse(input); process.exit(dependencies["@vyrnforge/ui-core"] === process.env.RELEASE_VERSION && dependencies["@vyrnforge/ui-behaviors"] === process.env.RELEASE_VERSION ? 0 : 1); })'
              for package_name in @vyrnforge/ui-core @vyrnforge/ui-behaviors @vyrnforge/ui-components @vyrnforge/ui-elements; do
                npm view "$package_name" dist-tags --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const tags = JSON.parse(input); process.exit(tags[process.env.RELEASE_TAG] === process.env.RELEASE_VERSION ? 0 : 1); })'
              done
              exit 0
            fi
            sleep 5
          done
          echo "non-grid beta publication did not propagate with expected dependencies and dist-tags"
          exit 1

      - name: Publish ui-data-grid through npm OIDC
        if: inputs.release-group == 'data-grid-alpha'
        run: npm publish ./packages/ui-data-grid --access public --tag "$RELEASE_TAG"

      - name: Verify ui-data-grid registry propagation and alpha dist-tag
        if: inputs.release-group == 'data-grid-alpha'
        shell: bash
        run: |
          BETA_VERSION="$(node --input-type=module -e 'import { readReleaseGroups } from "./scripts/release-groups.mjs"; console.log(readReleaseGroups().groups["non-grid-beta"].version);')"
          export BETA_VERSION
          for _ in {1..12}; do
            if test "$(npm view "@vyrnforge/ui-data-grid@$RELEASE_VERSION" version --registry=https://registry.npmjs.org 2>/dev/null || true)" = "$RELEASE_VERSION"; then
              npm view "@vyrnforge/ui-data-grid@$RELEASE_VERSION" dependencies --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const dependencies = JSON.parse(input); process.exit(dependencies["@vyrnforge/ui-core"] === process.env.BETA_VERSION && dependencies["@vyrnforge/ui-components"] === process.env.BETA_VERSION ? 0 : 1); })'
              npm view "@vyrnforge/ui-data-grid" dist-tags --json --registry=https://registry.npmjs.org | node -e 'let input=""; process.stdin.on("data", chunk => input += chunk).on("end", () => { const tags = JSON.parse(input); process.exit(tags[process.env.RELEASE_TAG] === process.env.RELEASE_VERSION ? 0 : 1); })'
              exit 0
            fi
            sleep 5
          done
          echo "ui-data-grid alpha publication did not propagate with expected beta dependencies and dist-tag"
          exit 1

      - name: Write publication summary
        shell: bash
        run: |
          if test "$RELEASE_GROUP" = "non-grid-beta"; then
            packages="ui-core, ui-behaviors, ui-components, ui-elements"
          else
            packages="ui-data-grid"
          fi
          {
            echo "## VyrnForge npm publication"
            echo
            echo "- Release group: \`$RELEASE_GROUP\`"
            echo "- Candidate version: \`$RELEASE_VERSION\`"
            echo "- Dist-tag: \`$RELEASE_TAG\`"
            echo "- Commit: \`$GITHUB_SHA\`"
            echo "- Published in order: $packages"
            echo "- Registry propagation and exact manifest dependencies verified"
            echo "- Credentials: GitHub OIDC trusted publishing only; no token secret used"
            echo "- Provenance: generated automatically by npm trusted publishing"
          } >> "$GITHUB_STEP_SUMMARY"

  verify-registry-release:
    name: verify-registry-release
    if: inputs.mode == 'publish'
    needs: publish-packages
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    env:
      RELEASE_GROUP: ${{ inputs.release-group }}
      RELEASE_VERSION: ${{ inputs.version }}
      RELEASE_TAG: ${{ inputs.dist-tag }}

    steps:
      - name: Checkout published commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Use repository package manager
        run: npm install --global "npm@$(node -p 'require("./package.json").packageManager.split("@").pop()')"

      - name: Verify registry metadata and fresh consumer
        run: node scripts/verify-registry-release.mjs --release-group "$RELEASE_GROUP" --version "$RELEASE_VERSION" --dist-tag "$RELEASE_TAG"

      - name: Write registry verification summary
        run: |
          {
            echo "## Registry and consumer verification"
            echo
            echo "- Release group: \`$RELEASE_GROUP\`"
            echo "- Exact version: \`$RELEASE_VERSION\`"
            echo "- Authoritative prerelease tag: \`$RELEASE_TAG\`"
            echo "- Package metadata and exact dependency graph verified"
            echo "- npm registry signatures and provenance attestations verified"
            echo "- Fresh registry consumer typecheck and production build passed"
            echo "- Any registry-managed \`latest\` tag is informational during prerelease; consumers must use the explicit prerelease tag"
          } >> "$GITHUB_STEP_SUMMARY"

  create-release-record:
    name: create-release-record
    if: inputs.mode == 'publish'
    needs: verify-registry-release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      RELEASE_GROUP: ${{ inputs.release-group }}
      RELEASE_VERSION: ${{ inputs.version }}
      RELEASE_TAG: ${{ inputs.dist-tag }}
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: Checkout published commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .nvmrc
          package-manager-cache: false

      - name: Confirm release record does not already exist
        shell: bash
        run: |
          TAG="v$RELEASE_VERSION"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Git tag already exists: $TAG"
            exit 1
          fi
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "GitHub Release already exists: $TAG"
            exit 1
          fi

      - name: Generate release notes
        run: node scripts/create-release-notes.mjs --release-group "$RELEASE_GROUP" --version "$RELEASE_VERSION" --dist-tag "$RELEASE_TAG" --commit "$GITHUB_SHA" --output release-notes.md

      - name: Create annotated tag and GitHub prerelease
        shell: bash
        run: |
          TAG="v$RELEASE_VERSION"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" "$GITHUB_SHA" -m "VyrnForge UI $RELEASE_GROUP $RELEASE_VERSION"
          git push origin "$TAG"
          if ! gh release create "$TAG" \
            --target "$GITHUB_SHA" \
            --title "VyrnForge UI $RELEASE_GROUP $RELEASE_VERSION" \
            --prerelease \
            --notes-file release-notes.md; then
            git push origin ":refs/tags/$TAG" || true
            exit 1
          fi

      - name: Write release-record summary
        run: |
          {
            echo "## GitHub release record"
            echo
            echo "- Release group: \`$RELEASE_GROUP\`"
            echo "- Tag: \`v$RELEASE_VERSION\`"
            echo "- GitHub Release: created as prerelease"
            echo "- Permission separation: this job has repository write access and no npm OIDC permission"
          } >> "$GITHUB_STEP_SUMMARY"
