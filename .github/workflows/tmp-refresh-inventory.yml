name: Temporary consumer inventory refresh

on:
  push:
    branches:
      - feat/consumer-knowledge-playground

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/consumer-knowledge-playground
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
      - name: Refresh inventory
        run: |
          npm run inventory:repository
          rm .github/workflows/tmp-refresh-inventory.yml
      - name: Commit inventory
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "chore: refresh consumer repository inventory"
          git push origin HEAD:feat/consumer-knowledge-playground
