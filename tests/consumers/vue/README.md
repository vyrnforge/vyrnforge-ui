# Vue consumer architecture fixture

This fixture records Vue compiler recognition for `vf-*` Custom Elements,
property/event binding, slots, and the future `v-model` adapter contract.

It is an S4 contract fixture, not a runtime support claim. A real Vue build,
model adapter suite, and browser matrix become mandatory at GMF4.
